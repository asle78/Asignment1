var React = require('react');

var App = React.createClass({
    render: function() {
        return (
            <div>
                <h1>This should display on the skjerm </h1>
            </div>
        );
    }
});

module.exports = App;